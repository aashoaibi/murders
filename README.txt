This is a training about how to use unix

Download-data.R: downloads cvs file to the directory

Wrangle-data.R:creates a driven dataset and saves as R object in rda directory 

analysis.R:generates a plot and save it to figs directory
